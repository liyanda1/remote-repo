# 2026-04-21 工作日志

## 生成增量修改指南

生成 `docs/opencode-adaptation-guide.md`，记录所有 OpenCode 适配的增量修改内容：

- **新增文件**：`.opencode/commands/` 下 6 个 command 文件（完整内容）、`.opencode/agents/` 下 2 个 agent 文件（完整内容）
- **修改文件**：`skills/sdd-task-design/SKILL.md` 的 4 处修改（Checklist 更新、插入 Step 7、Step 8 正文更新、集成说明更新）
- 包含执行顺序建议和 bash 验证命令，可在其他环境按图索骥执行增量迁移

## 审查阶段新增多模型审核机制

改造 `skills/sdd-task-review/SKILL.md`（8 处修改），将单模型审查改为可选多模型并行审查：

- 配置方式：仅 `opencode.json` 的 `review.models` 字段（≥2 个模型启用，不配或 <2 则走原流程）
- 冲突策略：任一 FAIL → 整体 FAIL
- Step 1.5 检查配置 → Step 3 单/多分支派发 → Step 4 单/多分支解析
- 增量修改指南已更新（第二部分）

## 审查模板简化

去掉审查提示模板的外部配置（`prompts/review-prompt.md`），直接使用 SKILL.md 内置模板：

- 删除原 Step 2（构建子 Agent 提示，含外部模板检查逻辑）
- Checklist 从 8 步缩为 7 步，Step 编号顺延（原 Step 1.5 → Step 2）
- 所有交叉引用统一更新

## 新增"已归档 AR 返工"入口

新增 `/sdd-reactivate` command，支持对已归档 AR 的增量返工：

- **新文件**：`.opencode/commands/sdd-reactivate.md`
  - 输入：`/sdd-reactivate <AR编号> <返工说明>`
  - 步骤：验证 AR 存在 → 移动 archive→changes → 解析返工意图确定重置阶段 → 执行重置 → 路由到对应阶段
  - 重置阶段判断：需求→requirements，设计→design，代码→develop，测试→st；多阶段修改以最早阶段为准
- **修改文件**：`skills/sdd-router/SKILL.md` — 末尾增加归档 AR 返工提示 + 新增"归档 AR 返工"章节
- **增量指南**：`docs/opencode-adaptation-guide.md` 追加第三部分（含完整 command 内容 + 1 处 router 修改 + 验证命令）

## 新增工程质量测试集（eval.json）

创建 `tests/eval.json`，覆盖 7 大类 32 个测试用例：

| 类别 | 数量 | 覆盖点 |
|------|------|--------|
| router | 8 | 各阶段状态检测 + 归档/返工提示 |
| requirements | 3 | 领域知识前置、验收标准、HARD-GATE |
| design | 3 | srs.md 驱动、组件详设刷新（需/不需） |
| develop | 4 | 单任务、TDD 顺序、依赖、质量门禁 |
| review | 5 | 子 Agent、三维完整性、单/多模型、FAIL 处理 |
| ST | 3 | 黑盒验证、Go/No-Go、归档内容 |
| reactivate | 6 | 目录移动、阶段路由、错误处理、多阶段取最早 |

增量指南第四部分已追加。
