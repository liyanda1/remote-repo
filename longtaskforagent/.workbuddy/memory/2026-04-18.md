# 2026-04-18 工作日志

## 新增 OpenCode Command 入口

为 SDD 流程 skill 集添加了 6 个 `/sdd*` slash command，存放于 `.opencode/commands/`：

| 文件 | Command | 职责 |
|------|---------|------|
| sdd.md | `/sdd` | 主入口，自动检测状态并路由 |
| sdd-req.md | `/sdd-req` | 强制进入需求澄清阶段 |
| sdd-design.md | `/sdd-design` | 强制进入详细设计阶段 |
| sdd-dev.md | `/sdd-dev [任务ID]` | 强制进入开发阶段，可指定任务 |
| sdd-review.md | `/sdd-review` | 强制进入合规审查阶段 |
| sdd-st.md | `/sdd-st` | 强制进入 ST 验收测试 + 归档阶段 |

README.md 中新增 "OpenCode Command 入口" 章节，包含安装说明、command 列表、推荐用法和 opencode.json 配置方式。

工程结构现增加 `.opencode/commands/` 目录，共 6 个 command 文件。

## 新增 OpenCode Agent 定义

在 `.opencode/agents/` 下新增两个专属代理：

| 文件 | 代理 | 模式 | 权限 |
|------|------|------|------|
| sdd-build.md | `@sdd-build` | primary | 全工具开放，sdd-* skill allow |
| sdd-reviewer.md | `@sdd-reviewer` | subagent | 全工具关闭（只读），skill deny |

README.md 重构为"OpenCode 适配"总章节，包含 Command、Agent、skill 独立部署三个小节。
plugin 不适配（OpenCode plugin 是 JS/TS 工具扩展，本流程无需新增原生工具）。
